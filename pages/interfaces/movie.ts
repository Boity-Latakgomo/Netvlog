export interface movieProps {
    title: string,
    genre: string,
    duration: string,
    starring: string,
    image: string,
    trailer: string,
    video: string,
    views: number, // not added on back-end
    releaseDate: Date, // not added on back-end
}